version: "3.9"

services:
  calendar-bot:
    build: .
    container_name: google-calendar-parser
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./token.json:/app/token.json
    restart: unless-stopped
